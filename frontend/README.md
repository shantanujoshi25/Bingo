# Bingo Frontend

React + TypeScript + Tailwind CSS, built with Vite.

```bash
npm install
npm run dev
```

Proxies `/api` to `localhost:8000` in dev mode. In production, FastAPI serves the built frontend directly.
